# Interactive Periodic Table

Just a project.

An interactive periodic table built with HTML, CSS, and JavaScript.

- Hover over an element to see its atomic number, mass, group, period, block, and category
- Click an element to open its Wikipedia page
- Color-coded by element category with a legend

## Usage

Open `index.html` in a browser. No build step, no dependencies.
